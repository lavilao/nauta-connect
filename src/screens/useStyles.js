import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  logo: {
    maxHeight: 80,
    maxWidth: 80,
    marginTop: 10,
  },
  logoContainer: {
    textAlign: 'center',
  },
  buttonsContainer: {
    marginTop: 30,
  },
  ShareButtons: {
    marginTop: 20,
    textAlign: 'center',
  },
}));

export default useStyles;
